import { Form } from 'antd'
import React, { ReactElement } from 'react'

interface Props {
  portfolio: any;
}

export default function PortfolioImportForm({portfolio}: Props): ReactElement {
  return (
  <Form>Portfolio</Form>
  )
}
