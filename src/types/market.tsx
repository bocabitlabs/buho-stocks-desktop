export interface MarketItemProps {
  id?: string;
  name: string;
  description: string;
  region: string;
  open_time: string;
  close_time: string;
}

export interface MarketFields {
  id: string;
  name: string;
  description: string;
  region: string;
  open_time: string;
  close_time: string;
}